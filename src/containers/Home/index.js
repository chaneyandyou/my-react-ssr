import React from 'react'

const Home = () => {
  return (
    <div>
      Home component, this is chaney
      <button onClick={() => { alert('click') }}>click</button>
    </div>
  )
}

export default Home